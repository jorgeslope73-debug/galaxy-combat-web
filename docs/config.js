/*
  GALAXY COMBAT WEB - CONFIGURACION

  Para GitHub Pages pon aqui la URL HTTPS de tu servidor de partida.
  Ejemplo:
    serverUrl: 'https://galaxy-combat-servidor.onrender.com'

  Si se deja vacio, el juego intenta usar el mismo servidor que sirve la web.
  Eso es util al probar todo localmente, pero GitHub Pages necesita un backend externo.
*/
window.GALAXY_CONFIG = {
  serverUrl: ''
};
