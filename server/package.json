{
  "name": "galaxy-combat-backend",
  "version": "1.2.0",
  "private": true,
  "description": "Backend WebSocket autoritativo de Galaxy Combat Web",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check room.js && node --check network.js && node --check physics.js && node --check gameplay.js && node --check security.js && node --check transport.js"
  },
  "dependencies": {
    "ws": "8.18.3"
  },
  "engines": {
    "node": ">=20"
  }
}
